#include <stdio.h>

int main() {
  // Creating array with length (3) and inserting into it nums
  int nums[3] = {1, 2, 3};
  
  // Printing nums array
  for (int i = 0; i < 3; i++) {
    printf("%d\n", nums[i]);
  }
  
  return 0;
}
